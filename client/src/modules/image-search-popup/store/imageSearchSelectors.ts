import {RootState} from "storage/reduxTypes";

export const imageSearchRoutes = (state: RootState) => state.imgSearchRoutes.new_routes;
export const userFile = (state: RootState) => state.imgSearchRoutes.file;